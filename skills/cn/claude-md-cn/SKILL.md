---
name: claude-md-cn
description: 生成或精简 CLAUDE.md，核实仓库命令、架构边界与特殊约定，仅保留能避免具体错误的规则。
disable-model-invocation: true
license: Apache-2.0
metadata:
  language: zh-CN
  translation_of: claude-md
  version: "0.1.0"
---

# CLAUDE.md

读取既有说明、构建脚本、CI 与相关源码，核实后按以下模板生成或更新 CLAUDE.md。
各项按需填写；无依据或不适用的项省略，保留必要条件与例外。

## Build / Test / Format

使用<统一入口>，不要直接运行<错误命令>，因为<原因>；<例外>除外。
构建：<命令>；单测：<命令>；集成测试：<命令及前提、覆盖范围>。
修改<文件类型>后运行<生成或格式化命令>；提交前运行<检查命令>。
<特殊任务>详见<文档路径>。

## Architecture

本仓库负责<范围>；<易混淆功能>位于<其他位置>。
<模块>负责<职责>，不得依赖<模块>。
<跨模块或进程通信>的契约与实现入口位于<路径>。
默认使用<实现>，仅在<条件>下使用<替代实现>。

## Conventions

<路径>是生成产物，禁止手工编辑；修改<源文件>。
新增实现放在<最小适用模块>。
遵循<配置或贡献文档>；注意<工具未自动处理的特殊约定>。

## Requirements

<构建、测试或运行场景>必需<版本、平台或工具>；不支持<已知边界>。
<特殊配置>仅适用于<条件>。

逐条自问：“删掉它，AI 会具体做错什么？”
没有明确防错价值的句子先删除；合并重复内容，不保留占位符或空栏目。
既有约束无法核实时，保留并报告；核实过时的规则修正或删除。
